apiVersion: apps/v1
kind: Deployment
metadata:
  name: gui
  namespace: prometheus
spec:
  template:
    spec:
      volumes:
      - name: prometheus
      containers:
      - name: prometheus
        image: prometheus-klot-io
        imagePullPolicy: Never
